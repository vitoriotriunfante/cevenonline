name: CEVEN NOC — Sync & Dispatch Automático

on:
  schedule:
    # =========================================
    # HORÁRIOS OFICIAIS UTC (Brasília = UTC - 3)
    # =========================================
    # 07:00 BRT = 10:00 UTC — Resumo Executivo de Abertura
    - cron: '0 10 * * 1-6'
    # 08:00 BRT = 11:00 UTC — Sync TV
    - cron: '0 11 * * 1-6'
    # 09:00 BRT = 12:00 UTC — Sync TV
    - cron: '0 12 * * 1-6'
    # 10:00 BRT = 13:00 UTC — Sync TV
    - cron: '0 13 * * 1-6'
    # 11:00 BRT = 14:00 UTC — Relatório Oficial Consolidado (WhatsApp)
    - cron: '0 14 * * 1-6'
    # 12:00 BRT = 15:00 UTC — Sync TV
    - cron: '0 15 * * 1-6'
    # 13:00 BRT = 16:00 UTC — Sync TV
    - cron: '0 16 * * 1-6'
    # 14:00 BRT = 17:00 UTC — Sync TV
    - cron: '0 17 * * 1-6'
    # 14:30 BRT = 17:30 UTC — Relatório Oficial Consolidado (WhatsApp)
    - cron: '30 17 * * 1-6'
    # 15:00 BRT = 18:00 UTC — Sync TV
    - cron: '0 18 * * 1-6'
    # 16:00 BRT = 19:00 UTC — Sync TV
    - cron: '0 19 * * 1-6'
    # 17:00 BRT = 20:00 UTC — Relatório Oficial Consolidado (WhatsApp)
    - cron: '0 20 * * 1-6'
    # 18:00 BRT = 21:00 UTC — Sync TV
    - cron: '0 21 * * 1-6'
    # 18:30 BRT = 21:30 UTC — Fechamento Oficial do Dia (WhatsApp)
    - cron: '30 21 * * 1-6'
    # 23:00 BRT = 02:00 UTC (+1d) — Cravar Fechamento Noturno Definitivo no D1
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      tipo:
        description: 'Tipo de disparo manual (ou dia especial)'
        required: false
        default: 'sync_tv_horario'
        type: choice
        options:
          - abertura_07h
          - relatorio_11h
          - relatorio_14h30
          - relatorio_17h
          - fechamento_18h30
          - fechamento_23h
          - sync_tv_horario
          - relatorio_consolidado
          - sync_cadastros

env:
  CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  CLOUDFLARE_D1_DATABASE_ID: '2e5bb59c-8db4-43a2-a13c-5c78e404e52d'

jobs:
  sync_and_dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar Wrangler CLI
        run: npm install -g wrangler

      - name: Determinar tipo de operação
        id: tipo
        run: |
          HORA_UTC=$(date -u +"%H:%M")
          echo "hora_utc=$HORA_UTC" >> $GITHUB_OUTPUT

          # Mapear hora UTC para tipo de disparo
          if [ "${{ github.event.inputs.tipo }}" != "" ]; then
            TIPO="${{ github.event.inputs.tipo }}"
          elif [ "$HORA_UTC" = "10:00" ]; then
            TIPO="abertura_07h"
          elif [ "$HORA_UTC" = "14:00" ]; then
            TIPO="relatorio_11h"
          elif [ "$HORA_UTC" = "17:30" ]; then
            TIPO="relatorio_14h30"
          elif [ "$HORA_UTC" = "20:00" ]; then
            TIPO="relatorio_17h"
          elif [ "$HORA_UTC" = "21:30" ]; then
            TIPO="fechamento_18h30"
          elif [ "$HORA_UTC" = "02:00" ]; then
            TIPO="fechamento_23h"
          else
            TIPO="sync_tv_horario"
          fi

          # Determinar se é sync de dados ou só disparo
          if [ "$TIPO" = "abertura_07h" ] || [ "$TIPO" = "sync_cadastros" ]; then
            PIPELINE="abertura"
          else
            PIPELINE="intraday"
          fi

          echo "tipo=$TIPO" >> $GITHUB_OUTPUT
          echo "pipeline=$PIPELINE" >> $GITHUB_OUTPUT
          echo "📊 Tipo: $TIPO | Pipeline: $PIPELINE | Hora UTC: $HORA_UTC"

      - name: 🔄 Executar Pipeline de Coleta (519 RCAs)
        run: |
          echo "🚀 Iniciando coleta de dados dos 519 RCAs..."
          echo "Pipeline: ${{ steps.tipo.outputs.pipeline }}"

          DATA_BR=$(TZ='America/Sao_Paulo' date +%Y-%m-%d)
          HORA_BR=$(TZ='America/Sao_Paulo' date +%H:%M)

          node pipeline/github_actions_runner.js \
            --pipeline "${{ steps.tipo.outputs.pipeline }}" \
            --data "$DATA_BR" \
            --hora "$HORA_BR"

      - name: 📤 Sincronizar dados com Cloudflare D1
        run: |
          if [ -f "d1_github_sync.sql" ]; then
            echo "📦 Enviando dados para o Cloudflare D1..."
            wrangler d1 execute ceven_noc_d1 \
              --remote \
              --file=d1_github_sync.sql \
              --config=wrangler.toml
            echo "✅ D1 atualizado com sucesso!"
          else
            echo "⚠️ Nenhum arquivo SQL gerado. Pulando sync do D1."
          fi

      - name: 📱 Disparar Mensagens WhatsApp
        run: |
          TIPO="${{ steps.tipo.outputs.tipo }}"
          echo "📱 Disparando WhatsApp: $TIPO"

          # Chamar o endpoint do Cloudflare Pages para enviar as mensagens
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            "https://ceven-cftv-matrix.pages.dev/api/cron-whatsapp-dispatcher?tipo=$TIPO")

          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -n -1)

          echo "HTTP Status: $HTTP_CODE"
          echo "$BODY" | head -c 2000

          if [ "$HTTP_CODE" != "200" ]; then
            echo "⚠️ Disparo retornou HTTP $HTTP_CODE"
          fi

      - name: 📊 Relatório de Execução
        if: always()
        run: |
          echo "=========================================="
          echo "📊 RELATÓRIO DE EXECUÇÃO"
          echo "=========================================="
          echo "Tipo: ${{ steps.tipo.outputs.tipo }}"
          echo "Pipeline: ${{ steps.tipo.outputs.pipeline }}"
          echo "Hora UTC: ${{ steps.tipo.outputs.hora_utc }}"
          echo "Status: ${{ job.status }}"
          echo "=========================================="
